{
  "type": "module",
  "devDependencies": {
    "@bytecodealliance/jco": "*",
    "@bytecodealliance/componentize-js": "*"
  },
  "scripts": {
    "transform": "jco transpile ./hello.component.wasm -o ./tmp",
    "build": "node componentize.js && npm run transform && cp ./tmp/hello.component.core.wasm hello.wasm",
    "test": "./target/release/wasmtime-test"
  }
}