import { print } from 'global';

export function echo(name) {
  return `Hello ${name} @@@@@@@@@@@@@@@@@@@@@@@@@@@@`;
}

export function add(a, b) {
  return a + b;
}

export function sub(a, b) {
  return a - b;
}