{
  "name": "docker-ts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist target",
    "build": "npx webpack --mode=production && mkdir -p target && spin js2wasm -o target/docker-ts.wasm dist/spin.js",
    "docker-build": " docker build --platform wasi/wasm --provenance=false -t docker.io/hpccjs/hello-docker-ts:latest .",
    "docker-run": "docker run -i --runtime=io.containerd.spin.v1 --platform=wasi/wasm -p 3000:80 docker.io/hpccjs/hello-docker:latest",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rimraf": "5.0.1",
    "ts-loader": "9.4.1",
    "typescript": "4.8.4",
    "webpack": "5.74.0",
    "webpack-cli": "4.10.0"
  },
  "dependencies": {
    "@fermyon/spin-sdk": "0.5.0"
  }
}